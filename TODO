Write the lexer
    - define all the tokens
    - elegant handling of errors in the lexer
        - return an error token and a provide callback for the parser to extract error message?
          the parser could also insert errors on top of lexer errors
    - keyword loopup table
